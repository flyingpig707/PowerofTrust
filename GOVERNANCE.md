# 共同写作治理

共同写作采用两级机制，保持开放参与与出版责任之间的边界。

## 一级：公开提案库

参与者在 `contributions/<github-login>/<proposal-slug>/` 提交结构化方案。自动检查只验证目录、必填字段、证据记录、授权声明和变更范围。通过检查的提案可以合并进入公开提案库，状态为 `proposed`，不代表作者认可或进入正式书稿。

维护者可根据讨论将状态标记为 `under-review` 或关闭不再推进的提案。内容评审关注：原创性、证据质量、概念边界、与全书框架的一致性、读者价值、表达质量、风险和出版权利。

## 二级：正式书稿编辑

作者或维护者决定采用某项贡献时，另行创建标记为 `editorial` 的编辑 Pull Request。该 PR 可以调整 `book/`，并必须：

- 链接原始提案和讨论；
- 说明采用、改写或未采用的范围；
- 更新 `CONTRIBUTORS.md`；
- 更新 `CHANGELOG.md`；
- 完成事实、版权和全书一致性的人工复核。

只有编辑 PR 合并后，相关内容才是正式书稿的一部分，状态才可记为 `incorporated`。

## 角色

- **贡献者**：提出问题、证据、案例、工具或文字方案，并对投稿来源和授权负责；
- **Agent**：协助研究、生成、整理、自检和准备 PR，但不能代表人类授权；
- **维护者**：管理仓库、讨论和确定性检查，不因合并提案库而背书内容；
- **作者/编辑**：对是否采用、如何改写以及最终出版内容负责。

## 状态

- `proposed`：进入提案流程，尚未完成内容评审；
- `under-review`：维护者正在评估；
- `selected`：计划采用全部或部分内容，仍可能编辑；
- `incorporated`：已通过独立编辑 PR 进入正式书稿。

自动化不得自行把状态改为 `selected` 或 `incorporated`，不得自动合并外部贡献。

